import { RequestRefreshToken } from './request-refresh-token';

describe('RequestRefreshToken', () => {
  it('should create an instance', () => {
    expect(new RequestRefreshToken()).toBeTruthy();
  });
});
